<Window
    x:Class="FirstTool.Views.ColumnPlacementInputView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    Title="Thiết lập cột phụ"
    Height="260"
    Width="380">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Family Type cột phụ" Margin="0,0,0,5"/>

        <ComboBox
            Grid.Row="1"
            x:Name="FamilyTypeComboBox"
            ItemsSource="{Binding FamilyTypes}"
            DisplayMemberPath="Name"
            SelectedItem="{Binding SelectedFamilyType}"
            Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="2" Text="Placement Mode:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <Grid Grid.Row="3" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <RadioButton
                    x:Name="BySpacingRadio"
                    GroupName="PlacementMode"
                    Content="Theo khoảng cách (mm)"
                    IsChecked="True"
                    Checked="ModeRadio_Checked"
                    Margin="0,0,0,5"/>

                <TextBox  x:Name="SpacingTextBox" Margin="0,0,0,15"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <RadioButton
                    x:Name="ByCountRadio"
                    GroupName="PlacementMode"
                    Content="Theo số lượng cột phụ"
                    Checked="ModeRadio_Checked"
                    Margin="0,0,0,5"/>

                <TextBox x:Name="CountTextBox" IsEnabled="False" Margin="0,0,0,15"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Height="30" Margin="0,0,10,0" Content="Huỷ" Click="CancelButton_Click"/>
            <Button Width="90" Height="30" Content="OK" Background="#1976D2" Foreground="White" Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>