<Window
    x:Class="FirstTool.Views.ColumnPlacementInputView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    Title="Thiết lập cột phụ"
    Height="220"
    Width="380">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Family Type cột phụ"
            Margin="0,0,0,5"/>

        <ComboBox
            Grid.Row="1"
            x:Name="FamilyTypeComboBox"
            ItemsSource="{Binding FamilyTypes}"
            DisplayMemberPath="Name"
            SelectedItem="{Binding SelectedFamilyType}"
            Margin="0,0,0,15"/>

        <TextBlock
            Grid.Row="2"
            Text="Khoảng cách bố trí (mm)"
            Margin="0,0,0,5"/>

        <TextBox
            Grid.Row="3"
            x:Name="SpacingTextBox"
            Text="{Binding SpacingInput, UpdateSourceTrigger=PropertyChanged}"
            Margin="0,0,0,15"/>

        <TextBlock
            Grid.Row="4"
            x:Name="ErrorText"
            Text="{Binding ErrorMessage}"
            Foreground="Red"
            TextWrapping="Wrap"
            VerticalAlignment="Top"/>

        <StackPanel
            Grid.Row="5"
            Orientation="Horizontal"
            HorizontalAlignment="Right">

            <Button
                Width="90"
                Height="30"
                Margin="0,0,10,0"
                Content="Huỷ"
                Click="CancelButton_Click"/>

            <Button
                Width="90"
                Height="30"
                Content="OK"
                Background="#1976D2"
                Foreground="White"
                Click="OkButton_Click"/>

        </StackPanel>
    </Grid>
</Window>